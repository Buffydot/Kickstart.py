name: Morning Kickstart

on:
  schedule:
    # Runs every day at 8 AM UTC
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  run_kickstart:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Kickstart Script
        run: python kickstart.py
        env:
          BOT_TOKEN: ${{7801214801:AAFzhtfW15FLztHWpVrVcTd_d4Mx1ugQRL0}}
          CHANNEL: ${{ https://t.me/my_chanell_pick_go }}
